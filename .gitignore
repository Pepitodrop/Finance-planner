node_modules
dist
*.tsbuildinfo
*.so
*.dll
*.dylib
.DS_Store
.env
.launch-evidence/
server/data/
server/*.env
.gstack/
public/vendor/transformers-3.8.1.min.js

# Provider private key material (e.g. Enable Banking's RSA key) must never be
# committed. deploy/enable-banking-private-key.placeholder is intentionally
# not a .pem file and stays tracked -- it holds no key material.
*.pem

# Android build output and local signing material.
android/.gradle/
android/local.properties
android/**/build/
*.jks
*.keystore
*.apk
*.aab

# Obsidian vault local/plugin state (the Markdown notes in FinancePlanner/ are intentionally tracked).
FinancePlanner/.obsidian/
